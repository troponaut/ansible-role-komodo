[project]
name = "ansible-role-komodo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ansible>=11.6.0",
]

[dependency-groups]
dev = [
    "ansible-lint>=25.4.0",
]
